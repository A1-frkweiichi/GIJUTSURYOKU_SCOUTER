<p><%= notice %><%= alert %></p>

<%# if user_signed_in? %>
  <%= link_to "Logout", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
<%# else %>
  <%= link_to "GitHub Login", login_path, method: :get, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
<%# end %>
